import { useState } from "react";
import { IQuestion } from "../../../../commons/types/generated/types";

export const PaginationsQuestionList = () => {
  const [startPage, setStartPage] = useState(1);
  const lastPage = Math.ceil(p);
};
